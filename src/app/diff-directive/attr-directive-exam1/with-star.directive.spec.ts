import { WithStarDirective } from './with-star.directive';

describe('WithStarDirective', () => {
  it('should create an instance', () => {
    const directive = new WithStarDirective();
    expect(directive).toBeTruthy();
  });
});
