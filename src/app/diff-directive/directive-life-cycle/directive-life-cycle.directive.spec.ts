import { DirectiveLifeCycleDirective } from './directive-life-cycle.directive';

describe('DirectiveLifeCycleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveLifeCycleDirective();
    expect(directive).toBeTruthy();
  });
});
