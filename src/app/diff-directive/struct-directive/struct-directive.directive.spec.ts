import { StructDirectiveDirective } from './struct-directive.directive';

describe('StructDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StructDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
