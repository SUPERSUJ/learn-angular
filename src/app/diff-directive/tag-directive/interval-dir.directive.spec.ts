import { IntervalDirDirective } from './interval-dir.directive';

describe('IntervalDirDirective', () => {
  it('should create an instance', () => {
    const directive = new IntervalDirDirective();
    expect(directive).toBeTruthy();
  });
});
