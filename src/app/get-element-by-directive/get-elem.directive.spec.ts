import { GetElemDirective } from './get-elem.directive';

describe('GetElemDirective', () => {
  it('should create an instance', () => {
    const directive = new GetElemDirective();
    expect(directive).toBeTruthy();
  });
});
